# Gradle / Android Studio
.gradle/
build/
local.properties
*.iml
.idea/
captures/
.externalNativeBuild/
.cxx/

# Bridging artifacts
bindings/
target/
# JNI .so libraries are regenerated by ./scripts/build-jni.sh — see README.
# (The high-level Kotlin bindings file at app/src/main/java/uniffi/aegis_ffi/
# is committed for review visibility, mirroring aegis-apple's policy.)
app/src/main/jniLibs/

# macOS dev hosts
.DS_Store

# Keystores must never be committed
*.jks
*.keystore
